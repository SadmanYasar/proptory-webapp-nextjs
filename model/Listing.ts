import { prisma } from "@/db";
import { Role } from "@prisma/client";

class Listing {
    constructor() { }

    addListing() {
        // zod schema
        //prisma add listing
    }

    updateListing() {
        // zod schema
        //prisma update listing
    }

    deleteListing() {
        // zod schema
        //prisma delete listing
    }

    getListingById() {
        //prisma get listing
    }

    getListings() {
        //prisma get listings
    }

    // rest
}

export default Listing;